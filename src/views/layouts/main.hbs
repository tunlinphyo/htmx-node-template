<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  {{#each cssPath as |css|}}
    <link rel="stylesheet" href="{{css}}">
  {{/each}}
  {{#each scriptPath as |js|}}
    <script src="{{js}}" defer></script>
  {{/each}}
</head>
<body>
  {{{body}}}
</body>
</html>